import 'package:flutter/material.dart';

class BorrowsScreen extends StatelessWidget {
  const BorrowsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
                child: Text('Borrows'),
              );
  }
}